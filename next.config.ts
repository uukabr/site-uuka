import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["useful-baseball-657fcd5d5a.media.strapiapp.com"],
    qualities: [100],
  },
  devIndicators: false,
};

export default nextConfig;
